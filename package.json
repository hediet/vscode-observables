{
  "name": "@vscode/observables-workspace",
  "private": true,
  "version": "1.0.0",
  "description": "Observable utilities for VS Code",
  "scripts": {
    "preinstall": "node -e \"if (!process.env.npm_config_user_agent?.startsWith('pnpm')) { console.warn('\\x1b[33m⚠ Warning: Please use pnpm instead of npm/yarn. Run: pnpm install\\x1b[0m'); process.exit(1); }\"",
    "build": "pnpm run build --workspaces --if-present",
    "dev": "concurrently \"pnpm run dev -w observables\" \"pnpm run dev -w observables-react\" \"pnpm run dev -w example-observables-react\"",
    "clean": "pnpm run clean --workspaces --if-present",
    "typecheck": "pnpm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "concurrently": "^8.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}